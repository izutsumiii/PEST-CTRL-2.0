{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch PEST-CTRL in Chrome",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000/index.php",
            "webRoot": "${workspaceFolder}"
        }
    ]
}

